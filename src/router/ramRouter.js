// import { getMenus } from '../api/user_roles';

// 首先在登陆成功之前，获取到所有的权限列表，
// 登陆成功时返回的id是否匹配权限列表中的某个id
// 当id匹配某一项时，(id=31),将这个权限列表数组中的某一项返回并传递到store中
// 设置动态路由的路由元信息，在每次页面加载前判断是否带有(meta:{id:xxx})信息
// 如果没有则阻止进入，并，如果拥有则继续下一步
